# Build
build/
bin/
*.o
*.a
*.so
*.dylib
compile_commands.json

# Generated traces / outputs
out.json
test_trace.json
ui/trace.json
*.trace.json

# City extracts are committed now, so the demo works from a clone with no
# network access. Only bulk source data stays out.
data/benchmark/
*.osm.pbf

# Tooling
__pycache__/
.venv/
.cache/
.DS_Store
web/engine/
site/
python/agss/_agss*.so
*.egg-info/
_skbuild/
